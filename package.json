{
  "name": "keyifyer",
  "version": "0.1.0",
  "description": "A better way to handle key events",
  "main": "index.js",
  "scripts": {
    "test": "node test/test.js",
    "test-coveralls": "istanbul cover test/test.js  && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage",
    "jshint": "jshint test/* index.js",
    "coverage": "rm -rf coverage && istanbul cover test/test.js && istanbul report html && serve coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tillarnold/keyifyer"
  },
  "keywords": [],
  "author": "Till Arnold",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "~3.0.0",
    "istanbul": "~0.4.5",
    "jshint": "^2.5.3",
    "serve": "^6.4.9",
    "tape": "^4.8.0"
  },
  "dependencies": {
    "invert-hash": "0.0.0",
    "vkey": "^1.0.0"
  }
}
